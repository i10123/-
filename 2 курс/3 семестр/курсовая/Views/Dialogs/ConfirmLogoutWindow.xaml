<Window x:Class="QR_generator.Views.Dialogs.ConfirmLogoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выход" Height="220" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True" ShowInTaskbar="False">

    <Border Background="#FF2D2D30" CornerRadius="0" BorderBrush="Red" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="5" Opacity="0.5"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <!-- Шапка -->
            <Border Background="Black" Grid.Row="0" MouseLeftButtonDown="DragWindow">
                <StackPanel Orientation="Horizontal">
                    <!-- Иконка выхода мелкая -->
                    <Path Data="M16,17V14H9V10H16V7L21,12L16,17M14,2H3A2,2 0 0,0 1,4V20A2,2 0 0,0 3,22H14A2,2 0 0,0 16,20V18H14V20H3V4H14V6H16V4A2,2 0 0,0 14,2Z" 
                          Fill="Red" Height="12" Width="12" Stretch="Uniform" Margin="10,0,5,0"/>
                    <TextBlock Text="Подтверждение" Foreground="White" FontWeight="Bold" 
                               VerticalAlignment="Center" FontSize="12"/>
                </StackPanel>
            </Border>

            <!-- Иконка + Текст -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <!-- Большой знак вопроса -->
                    <Path Data="M10,19H14V15H10V19M12,4A5,5 0 0,1 17,9C17,11.1 15.3,12.5 14,13.4V14H10V12.6C10,10.5 11.7,9.1 13,8.4C13.6,8.1 14,7.6 14,7A2,2 0 0,0 12,5A2,2 0 0,0 10,7H6A6,6 0 0,1 12,4Z" 
                          Fill="Red" Stretch="Uniform" Height="40" Width="40" Margin="0,0,15,0">
                        <Path.Effect>
                            <DropShadowEffect Color="Red" BlurRadius="10" ShadowDepth="0" Opacity="0.4"/>
                        </Path.Effect>
                    </Path>

                    <TextBlock x:Name="txtMessage" Text="Вы действительно хотите&#x0a;выйти из аккаунта?" Foreground="White" FontSize="16" VerticalAlignment="Center" TextAlignment="Left" LineHeight="22"/>
                </StackPanel>
            </StackPanel>

            <!-- Кнопки (Да / Нет) -->
            <Grid Grid.Row="2" Margin="20,0,20,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Кнопка ДА -->
                <Button Grid.Column="0" Content="ВЫЙТИ" Click="BtnYes_Click" Height="35" FontSize="14" FontWeight="Bold" Foreground="White" BorderBrush="Black">
                    <Button.Background>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                            <GradientStop Color="Black"/>
                            <GradientStop Color="#FF990000" Offset="1"/>
                        </LinearGradientBrush>
                    </Button.Background>
                </Button>

                <!-- Кнопка НЕТ -->
                <Button Grid.Column="2" Content="ОТМЕНА" Click="BtnNo_Click" Height="35" FontSize="14" Background="Transparent" Foreground="Gray" BorderBrush="Gray"/>
            </Grid>
        </Grid>
    </Border>
</Window>